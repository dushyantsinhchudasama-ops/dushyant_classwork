import { MovieTypePipePipe } from './movie-type-pipe-pipe';

describe('MovieTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MovieTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
